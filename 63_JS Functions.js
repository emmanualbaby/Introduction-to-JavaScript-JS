// JS Functions - Example
function myFunction(p1, p2) {
  return p1 * p2; // Function returns the product of p1 and p2
}

let result = myFunction(4, 3); // Call the function
console.log("Result: " + result); // Print the result
